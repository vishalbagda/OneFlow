const steps = ["Party details", "Agreement terms", "Stamp duty", "Payment", "e-Sign", "Confirmation"];
export function ProgressBar({ currentStep }: { currentStep: number }) { return <div aria-label={`Step ${currentStep} of 6 — ${steps[currentStep - 1]}`}><div className="flex justify-between text-xs font-medium text-slate"><span>Step {currentStep} of 6</span><span>{steps[currentStep - 1]}</span></div><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-hairline"><div className="h-full rounded-full bg-amber" style={{ width: `${(currentStep / steps.length) * 100}%` }} /></div></div>; }
